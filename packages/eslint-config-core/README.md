# @infinityloop.labs/eslint-config-core

Base ESLint flat-config shared by both frontend and backend presets. It wires up
TypeScript, Prettier, and import ordering without any framework-specific rules.
