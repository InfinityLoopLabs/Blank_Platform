# @infinityloop.labs/eslint-config-frontend

React-oriented ESLint flat-config that extends the shared core ruleset and adds
browser globals, React recommendations, and custom import ordering used across
frontend apps.
