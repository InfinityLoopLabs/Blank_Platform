{
  "name": "npm.piece",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "_templates/*",
    "frontend/*",
    "backend/*",
    "packages/*"
  ],
  "scripts": {
    "build:libs": "turbo run build --filter=@infinityloop.labs/constants-frontend & turbo run build --filter=@infinityloop.labs/event-bus & turbo run build --filter=@infinityloop.labs/common-frontend && turbo run build --filter=@infinityloop.labs/utils && turbo run build --filter=@infinityloop.labs/ui-kit  && turbo run build --filter=@infinityloop.labs/mf-connect  && turbo run build --filter=@infinityloop.labs/locale-downloader",
    "build": "turbo run build",
    "prestart": "yarn build:libs",
    "start": "turbo run dev --filter=app_auth & turbo run dev --filter=app_root",
    "test:watch": "turbo run test:watch",
    "format": "eslint --fix --ext .js,.jsx,.tsx,ts .",
    "mf": "npm.piece-connect-mf",
    "rmrf": "node deleteNodeModules.js",
    "update": "node ./libs/update-packages/bin/index.js && yarn"
  },
  "devDependencies": {
    "turbo": "^2.2.3"
  }

}
